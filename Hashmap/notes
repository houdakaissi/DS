  HashMap<Integer, String> hash_map = new HashMap<Integer, String>();  
        hash_map.put(1, "Red");
        hash_map.put(2, "Green");
        hash_map.put(3, "Black");
        hash_map.put(4, "White");
        hash_map.put(5, "Blue");
        
        // Iterating through the map entries and printing them
        for (Map.Entry<Integer, String> entry : hash_map.entrySet()) {  
            System.out.println(entry.getKey() + " " + entry.getValue());  
        }

        // Checking if the map contains a key
        System.out.println(hash_map.containsKey(6));
  //size()
  //copy hm1 to hm2 hash_map2.putAll(hash_map1);
  //clear 
  //isEmpty 
  //new_hash_map = (HashMap)hash_map.clone(); 
  // if contains a key containsKey("Green") or value containsValu
  
  // // create set view for the map hash_map.entrySet();
  //ge tvealue of a key hash_map.get(3); 
  //see all keys in set Set keyset = hash_map.keySet();
  //see all values hash_map.values()
  ---->>>put get entryset containsvalue containskey keyset values copt size isEmpty clear 
