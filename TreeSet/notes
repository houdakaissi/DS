
//add elements 
.add()   //size of treeset .size()    //remove element  .remove(element)   //5
//find max 	TreeSet<Integer> t=new TreeSet<>();
	t.add(1);
	t.add(2);
	int max=0;
	Iterator<Integer> i=t.iterator();
	while(i.hasNext()){
	    if(max<i.next()){
	        max=i.next();
	    }
	}   //isEmpty    //iterator in treeset show elements in acsending order
